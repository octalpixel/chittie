{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the companion window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
