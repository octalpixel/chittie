# chittie examples

- [`web/`](./web) — Web Serial + a `<canvas>` rasterizer (run with `npx vite`).
- [`react-native/`](./react-native) — BLE via `createBleTransport`, with notes for wiring
  react-native-ble-plx + a Skia rasterizer.

Both print a receipt with a Sinhala line to show automatic rasterization.
